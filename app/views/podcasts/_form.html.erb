<%= form_with(model: podcast, local: true) do |form| %>
  <% if podcast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(podcast.errors.count, "error") %> prohibited this podcast from being saved:</h2>

      <ul>
      <% podcast.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :podcast_title %>
  </div>

  <div class="field">
    <%= form.label :query %>
    <%= form.text_field :query, id: :podcast_query %>
  </div>

  <div class="field">
    <%= form.label :order_by %>
    <%= form.text_field :order_by, id: :podcast_order_by %>
  </div>

  <div class="field">
    <%= form.label :max_count %>
    <%= form.number_field :max_count, id: :podcast_max_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
